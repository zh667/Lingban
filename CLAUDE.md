# CLAUDE.md

@AGENTS.md

## Claude Code 补充

- 按 `AGENTS.md` 的文档地图渐进读取,不一次加载全部资料。
- 用户级个人偏好留在 Claude memory,不提交到仓库。
- 构建 MCP Server 前先调用 `mcp-builder` skill;新建或改版 UI 前先调用 `frontend-design` skill;UI 验证用 `webapp-testing` skill;处理 Excel 报表和 Word SOP 分别用 `xlsx` / `docx` skill。
